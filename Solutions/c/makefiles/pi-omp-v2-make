CC=cc
F90=ftn
PRGENV=${PE_ENV}
CFLAGS=-g
SOURCE=.

ifeq ($(PRGENV),INTEL)
	OMP_FLAG=-openmp
else ifeq ($(PRGENV),GNU)
	OMP_FLAG=-fopenmp
else ifeq ($(PRGENV),CRAY)
	OMP_FLAG=-homp
endif 

all: omp-v2 

omp-v2:
	$(CC) $(CFLAGS) $(SOURCE)/pi-omp-v2.c -o pi-omp-v2 $(OMP_FLAG)
clean:
	rm -rf pi-omp-v2
